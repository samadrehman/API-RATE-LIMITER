# CRITICAL SECURITY SETTINGS - MUST BE SET BEFORE PRODUCTION

# JWT Secret Key - Used for session encryption
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secure-secret-key-change-me-min-32-chars

# Admin Token - Required for all admin endpoints
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=your-secure-admin-token-change-me

# CORS CONFIGURATION

# Comma-separated list of allowed origins (no wildcards in production)
# Example: https://yourdomain.com,https://api.yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# DATABASE CONFIGURATION

# Database file path
DB_PATH=ratelimiter.db

# Connection pool size (increase for higher traffic)
DB_POOL_SIZE=5

# RATE LIMITING CONFIGURATION

# IP-based rate limiting
IP_RATE_LIMIT=100
IP_WINDOW=60

# Temporary ban duration in seconds
TEMP_BAN_SECONDS=300

# Ban multiplier for repeated violations
BAN_MULTIPLIER=2

# Maximum log entries to return in single query
MAX_LOG_ENTRIES=1000

# APPLICATION CONFIGURATION

# Flask environment: development or production
FLASK_ENV=development

# Maximum request body size in bytes (16KB default)
MAX_CONTENT_LENGTH=16384

# WebSocket async mode: threading or eventlet
SOCKETIO_ASYNC_MODE=threading

# NOTES
#
# 1. NEVER commit this file with real values to version control
# 2. Generate secure random tokens for production
# 3. Use HTTPS in production
# 4. Restrict CORS_ORIGINS to your specific domains
# 5. Consider using a secrets manager (Vault, AWS Secrets Manager) in production
# 6. Rotate JWT_SECRET_KEY and ADMIN_TOKEN regularly
# 7. Monitor access logs for suspicious activity
#
